<!-- GENERATED by frontend/site/gen-manifest.cjs — do not edit by hand. Run `npm run manifest`. -->

# DrupalX Canvas — component reference

The **registered** components (have a `canvas.component.sdc.drupalx_theme.*` config →
**available in the Canvas editor**). Author pages as data (`pages-data/*.cjs`) using these.
Prefer the helper where one exists; otherwise write `{ component, props, slots }` directly.

**Prop forms** (the bridge wraps them for Canvas; in data keep them PLAIN):
- image → `img(src, alt)` object · link/url → plain string · rich-text → plain HTML string (`rt()`).
- enum props must use one of the listed values — never `''`, use `'none'` (`npm run validate` checks this).

Helpers in `pages-data/_helpers.cjs`: `hero, pageHero, statBand, stat, cardGrid, card,
newsCard, eventCard, split, cta, richtext, quote, gallery, accordion, accordionItem,
alert, docList, docItem, button, heading, icon, image, divider, spacer, img, rt`.

---

### `accordion`  · helper: `accordion()`

Props:
  - `eyebrow` — string
  - `title` — string
  - `lede` — rich-text — plain HTML string (use `rt()`)
  - `tone` — enum: `white` / `tint`

Slots: `items` (each holds an array of child nodes)

### `accordion_item`  · helper: `accordionItem()`

Props:
  - `heading` **(required)** — string
  - `body` — rich-text — plain HTML string (use `rt()`)
  - `open` — boolean

### `alert_banner`  · helper: `alert()`

Props:
  - `message` **(required)** — rich-text — plain HTML string (use `rt()`)
  - `tone` — enum: `info` / `warning` / `emergency`
  - `alert_id` — string
  - `dismissible` — boolean

### `button`  · helper: `button()`

Props:
  - `label` **(required)** — string
  - `url` **(required)** — link — plain string (e.g. `/about`)
  - `variant` — enum: `primary` / `accent` / `ghost` / `glass`
  - `size` — enum: `small` / `normal` / `large`
  - `icon` — enum: `none` / `arrow-right` / `arrow-up-right` / `chart-column` / `building-2` / `calendar` / `check` / `chevron-down` / … (37 values)

### `card`  · helper: `card()`

Props:
  - `title` **(required)** — string
  - `body` — rich-text — plain HTML string (use `rt()`)
  - `url` — link — plain string (e.g. `/about`)
  - `image` — image — pass `img(src, alt)`
  - `icon` — enum: `none` / `arrow-right` / `arrow-up-right` / `chart-column` / `building-2` / `calendar` / `check` / `chevron-down` / … (37 values)
  - `c` — enum: `none` / `sky` / `clay` / `accent`
  - `more_label` — string

### `card_event`  · helper: `eventCard()`

Props:
  - `title` **(required)** — string
  - `month` — string
  - `day` — string
  - `time` — string
  - `location` — string
  - `url` — link — plain string (e.g. `/about`)
  - `c` — enum: `none` / `sky` / `clay` / `accent`

### `card_news`  · helper: `newsCard()`

Props:
  - `title` **(required)** — string
  - `date` — string
  - `summary` — rich-text — plain HTML string (use `rt()`)
  - `url` — link — plain string (e.g. `/about`)
  - `image` — image — pass `img(src, alt)`

### `divider`  · helper: `divider()`

Props:
  - `width` — enum: `content` / `prose`

### `document_item`  · helper: `docItem()`

Props:
  - `title` **(required)** — string
  - `description` — string
  - `url` — link — plain string (e.g. `/about`)
  - `filetype` — enum: `pdf` / `doc` / `xls` / `generic`
  - `meta` — string

### `document_list`  · helper: `docList()`

Props:
  - `eyebrow` — string
  - `title` — string
  - `more_label` — string
  - `more_url` — link — plain string (e.g. `/about`)
  - `tone` — enum: `white` / `tint`

Slots: `documents` (each holds an array of child nodes)

### `heading`  · helper: `heading()`

Props:
  - `title` **(required)** — string
  - `eyebrow` — string
  - `level` — enum: `h1` / `h2` / `h3` / `h4`
  - `style` — enum: `display` / `title` / `subtitle`
  - `align` — enum: `left` / `center`

### `icon`  · helper: `icon()`

Props:
  - `name` — enum: `none` / `arrow-right` / `arrow-up-right` / `chart-column` / `building-2` / `calendar` / `check` / `chevron-down` / … (37 values)
  - `size` — enum: `sm` / `md` / `lg`

### `image`  · helper: `image()`

Props:
  - `image` **(required)** — image — pass `img(src, alt)`
  - `aspect` — enum: `auto` / `16x9` / `4x3` / `square`
  - `rounded` — enum: `none` / `xl` / `2xl`
  - `frame` — enum: `none` / `offset-accent`
  - `caption` — string

### `page_hero`  · helper: `pageHero()`

Props:
  - `eyebrow` — string
  - `title` **(required)** — string
  - `lede` — rich-text — plain HTML string (use `rt()`)
  - `image` — image — pass `img(src, alt)`
  - `tone` — enum: `dark` / `light`

### `section_cards`  · helper: `cardGrid()`

Props:
  - `eyebrow` — string
  - `title` — string
  - `lede` — rich-text — plain HTML string (use `rt()`)
  - `columns` — enum: `2` / `3` / `4`
  - `tone` — enum: `white` / `tint`
  - `featured_first` — boolean

Slots: `cards` (each holds an array of child nodes)

### `section_cta`  · helper: `cta()`

Props:
  - `eyebrow` — string
  - `title` **(required)** — string
  - `body` — rich-text — plain HTML string (use `rt()`)

Slots: `cta_buttons` (each holds an array of child nodes)

### `section_gallery`  · helper: `gallery()`

Props:
  - `eyebrow` — string
  - `title` — string
  - `columns` — enum: `2` / `3`

Slots: `images` (each holds an array of child nodes)

### `section_hero`  · helper: `hero()`

Props:
  - `eyebrow` — string
  - `title` **(required)** — string
  - `body` — rich-text — plain HTML string (use `rt()`)
  - `image` — image — pass `img(src, alt)`
  - `style` — enum: `glass` / `split` / `dark`
  - `height` — enum: `short` / `medium` / `tall`
  - `stat_chip_number` — string
  - `stat_chip_label` — string

Slots: `hero_buttons` (each holds an array of child nodes)

### `section_quote`  · helper: `quote()`

Props:
  - `quote` **(required)** — rich-text — plain HTML string (use `rt()`)
  - `attribution` — string
  - `role` — string
  - `image` — image — pass `img(src, alt)`
  - `tone` — enum: `white` / `tint`

### `section_richtext`  · helper: `richtext()`

Props:
  - `body` **(required)** — rich-text — plain HTML string (use `rt()`)
  - `width` — enum: `prose` / `wide`
  - `tone` — enum: `white` / `tint`

### `section_split`  · helper: `split()`

Props:
  - `eyebrow` — string
  - `title` **(required)** — string
  - `body` — rich-text — plain HTML string (use `rt()`)
  - `image` — image — pass `img(src, alt)`
  - `media_side` — enum: `left` / `right`
  - `tone` — enum: `white` / `tint`

Slots: `split_buttons` (each holds an array of child nodes)

### `section_stats`  · helper: `statBand()`

Props:
  - `eyebrow` — string
  - `title` — string
  - `tone` — enum: `dark` / `light`
  - `columns` — enum: `2` / `3` / `4`

Slots: `stats` (each holds an array of child nodes)

### `spacer`  · helper: `spacer()`

Props:
  - `size` — enum: `sm` / `md` / `lg` / `xl`

### `stat`  · helper: `stat()`

Props:
  - `number` **(required)** — string
  - `label` **(required)** — string
  - `c` — enum: `none` / `sky` / `clay` / `accent`

